#Exportamos la función sqrt del bloque math

from math import sqrt

#Pedida de datos

numero = float(input('Introduce un número: '))

#Calculamos la raíz cuadrada del número y lo mostramos

print('La raíz cuadrada del número es: {0}'.format(sqrt(numero)))